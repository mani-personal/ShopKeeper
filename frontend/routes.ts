import {useEffect,useState} from 'react';
export const pagePaths:Record<string,string>={'Dashboard':'dashboard','Point of sale':'point-of-sale','Inventory':'inventory','Sales':'sales','Purchases':'purchases','Customers':'customers','Suppliers':'suppliers','Reports':'reports','Expenses':'expenses','Settings':'settings','Vendors':'vendors','Vendor access':'vendor-access','Subscription approvals':'subscription-approvals','Wholesale':'wholesale','Scan bill':'scan-bill','Account':'account','Supplier accounts':'supplier-accounts','Pricing':'pricing','Super admin':'admins'};
function current(){if(location.search.includes('plan='))return 'Pricing';if(!location.pathname.startsWith('/app/'))return 'Dashboard';return Object.keys(pagePaths).find(name=>'/app/'+pagePaths[name]===location.pathname)||'Page not found';}
export function usePageRoute():[string,(name:string)=>void]{const [view,setView]=useState(current);useEffect(()=>{const change=()=>setView(current());window.addEventListener('popstate',change);return()=>window.removeEventListener('popstate',change)},[]);useEffect(()=>{document.title=view+' · Shopkeeper';if(!location.pathname.startsWith('/app/'))history.replaceState(null,'','/app/'+(pagePaths[view]||'dashboard')+location.search)},[view]);return [view,name=>{if(!pagePaths[name])return;const params=new URLSearchParams(location.search);params.delete('plan');const path='/app/'+pagePaths[name]+(params.size?'?'+params.toString():'');if(location.pathname+location.search!==path)history.pushState(null,'',path);setView(name)}];}
